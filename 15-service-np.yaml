apiVersion: v1
kind: Service
metadata:
  name: nginx-np
spec:
  type: NodePort
  selector:
    project: roboshop
    component: frontend
    environment: dev
    purpose: node-port
  port:
    - protocol: TCP
      port: 80
      targetPort: 80